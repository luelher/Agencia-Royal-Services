<%= form_for(@municipio) do |f| %>
  <% if @municipio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@municipio.errors.count, "error") %> prohibited this municipio from being saved:</h2>

      <ul>
      <% @municipio.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %><br />
    <%= f.text_field :descripcion %>
  </div>
  <div class="field">
    <%= f.label :estado_id %><br />
    <%= f.select :estado_id, Estado.all.collect {|p| [ p.descripcion, p.id ] }, { :include_blank => false } %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
