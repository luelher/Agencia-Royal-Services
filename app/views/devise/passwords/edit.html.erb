<h2>Cambiar Contraseña</h2>

<%= form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :put }) do |f| %>
  <%= devise_error_messages! %>
  <%= f.hidden_field :reset_password_token, :value => User.reset_password_token %>

  <div><%= f.label :password, "Nueva Contraseña" %><br />
  <%= f.password_field :password %></div>

  <div><%= f.label :password_confirmation, "Confirme Nueva Contraseña" %><br />
  <%= f.password_field :password_confirmation %></div>

  <div><%= f.submit "Cambiar contraseña" %></div>
<% end %>
